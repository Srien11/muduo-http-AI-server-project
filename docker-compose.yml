version: "3.9"
services:
  zhimo:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./server.conf:/app/server.conf
      - ./memory:/app/memory
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

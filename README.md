# muduo-http-AI-server-project

基础项目结构：

- `src/` 源码目录
- `include/` 头文件目录
- `build/` 编译输出目录
- `CMakeLists.txt` CMake 构建脚本

## 构建运行

```bash
cmake -S . -B build
cmake --build build
./build/muduo_http_server
```
